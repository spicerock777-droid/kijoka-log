<% content_for :title, "新しい施工記録 - #{@project.name}" %>

<div class="md:w-2/3 w-full">
  <div class="mb-6">
    <%= link_to "← #{@project.name}", project_path(@project), class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="font-bold text-2xl text-green-900 mt-1">新しい施工記録</h1>
  </div>
  <%= render "form", construction_record: @construction_record %>
</div>
