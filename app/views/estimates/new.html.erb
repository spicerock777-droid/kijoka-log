<% content_for :title, "新しい見積書 - #{@project.name}" %>

<div class="w-full max-w-3xl">
  <div class="mb-6">
    <%= link_to "← #{@project.name}", project_path(@project), class: "text-sm text-gray-500 hover:text-gray-700" %>
    <h1 class="font-bold text-2xl text-green-900 mt-1">新しい見積書</h1>
  </div>
  <%= render "form", estimate: @estimate %>
</div>
